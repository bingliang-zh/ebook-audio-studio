PORT=4000
PUBLIC_BASE_URL=http://localhost:4000
MAX_UPLOAD_MB=50

# mock: returns a downloadable .txt placeholder that proves the pipeline works.
# macos: uses local /usr/bin/say and /usr/bin/afconvert to create .m4a audio.
TTS_PROVIDER=mock
