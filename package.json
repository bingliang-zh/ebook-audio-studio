{
  "name": "ebook-audio-studio",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.27.0",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "web:dev": "pnpm --filter client dev",
    "web:build": "pnpm --filter client build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.0",
    "typescript": "^5.8.3"
  }
}
